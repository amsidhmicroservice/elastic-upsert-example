server:
  port: 8181

management:
  endpoints:
    web:
      exposure:
        include: "*"
  health:
    elasticsearch:
      enabled: true

spring:
  elasticsearch:
    uris:
      - http://34.123.5.213:9200
    username:
    password:
    connection-timeout: 120s
    socket-timeout: 60s
    restclient:
      sniffer:
        interval: 5m

logging:
  level:
    tracer: TRACE
